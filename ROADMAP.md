# Ashura MDD ロードマップ

方針: **勝負は表現力ではなく同期コストで決まる**(MDDとSDDの死因より)。
よって測量器(drift検出)を最初に作り、価値検証を最短にする。

## Phase 0 — 言語の骨格(完了)

- [x] 自己モデル(`domain/ashura.model.md`)の初版
- [x] ケーススタディ4題でコア構文の当たりを付ける
- [x] コアDSLの文法定義(Langium)。ケーススタディの手書きMarkdownを機械可読の `.ashura` に移す(`packages/ashura-core`、すごろく1題を翻訳)
- [x] LSP検査の第一弾: 語彙整合 / 状態機械の網羅性 / フロー⇄集約の参照整合

## Phase 1 — サーベイヤー(三点測量の最小構成、最小構成のみ完了)

- [x] トレース表フォーマットの定義(モデル宣言 → コード位置の対応)
- [x] drift 検出(②実装からの推論 vs ①意図) — フィクスチャ駆動の最小構成(`packages/ashura-surveyor`)。
      実LLM配線・実プロジェクトへの適用は未着手(将来フェーズ送り)
- [x] 差分診断の決定表(自己モデル参照)の実装
- [x] 乖離ライフサイクル(検出→裁定待ち→解消、7日エスカレーション)

**合否基準**: 実プロジェクトで「人間に上がる裁定件数がドメイン変更に比例し、
コード行数に比例しない」ことを計測できる。← フィクスチャ駆動の最小構成では未計測。
実LLM配線・実プロジェクト適用のフェーズで検証する。

## Phase 2 — 検証系(アドバーサリー、最小構成のみ完了)

- [x] 性質宣言 → 検証タスク列挙 — フィクスチャ駆動の最小構成(`packages/ashura-surveyor`)。
      実際のプロパティベーステストコード(fast-check等)生成・実行は未着手(将来フェーズ送り)
- [x] 失敗セマンティクス宣言 → 検証タスク列挙。実際のフォールトインジェクション実行は未着手
- [x] 独立導出の担保(生成文脈と検証文脈の分離) — 検証タスク列挙の入力型を `ast.Model` のみに
      限定することで型的に保証(生成コードへの参照経路を持たせない)

## Phase 3 — 生成系(ジェネレーター+ファシリテーター、最小構成のみ完了)

- [x] 承認済みモデル+steering → コード生成+トレース表 — 生成ゲート(未承認モデル拒否)と
      トレース表完全性検査(宣言の丸ごと欠落も検出)はフィクスチャ駆動の最小構成で完了
      (`packages/ashura-surveyor`)。実LLMによるコード実体の生成は未着手(将来フェーズ送り)
- [x] 冪等性の検証(バイト同一ではなく検証結果等価) — Phase 2 の `runVerification` を
      再利用した検証結果集合の比較で実装
- [x] ファシリテーター: 禁止遷移が0件の集約を検出し質問アジェンダを生成する決定的な下地のみ
      完了。対話そのもの・「失敗系・境界規則」の検出(自由文字列解析が必要)は未着手

## Phase 4 — 方言機構(最小構成のみ完了)

- [x] 方言プラグイン仕様(三点セット: 構文糖衣+脱糖規則 / 追加LSP検査 / 検証アダプタ) —
      `DialectPlugin` インターフェースとして実装(`packages/ashura-dialect-ui`)。
      検証アダプタは Phase 1-3 と同型のフィクスチャシームで実ツール連携は未着手
- [x] 方言の憲法を自己モデルに追記(コア意味論を破れない / 脱糖必須 / 語彙統一) —
      横断制約に3項目(方言脱糖必須・コア意味論不可侵・測量器一元化)追記。
      ユーザー承認済み、コード実装とは別コミット
- [x] `ashura-dialect-ui`: trait(基底状態機械の取り込み)構文糖衣のみ完了。
      トークン・合成規則(O(n²)対応)は未着手(将来フェーズ送り)
- [x] サーベイヤーは脱糖後のコア語彙のみを見る(測量器は一つ) — 方言ソース→脱糖→
      コア `ast.Model` に対し、Phase 1-3 の既存サーベイヤー関数(`enumerateModelDeclarations`・
      `analyzeGaps`)をコード変更なしで適用できることを一気通貫テストで実証

## Phase 5 — ブートストラップ(成熟の儀式、最小構成のみ完了)

- [x] 自己モデルを `.ashura` 化し、サーベイヤー自身の測量対象に乗せる —
      `domain/ashura.model.ashura`。`文脈`によるネストはコアDSLで表現できないため
      フラットな要素列に翻訳(暗黙の対応を明示化: 契機トリガー用のイベントを最小限追加、
      冪等性/独立導出などフロー内不変条件は `検査`/`性質` へ再配置)。
      `domain/ashura.model.md` は廃止(ユーザー承認済み、二重管理を避けるため)。
      `packages/ashura-core` にパーステスト追加、診断ゼロ・要素14件で green
- [x] Ashura の開発プロセス自体が Ashura のライフサイクル(草稿→検査通過→レビュー中→承認済み)を通る —
      性質「自己適用性」の実証として、Phase 1-3 の既存サーベイヤー関数
      (`enumerateModelDeclarations`・`analyzeGaps`・`generate`)を自己モデルの実ASTに
      コード変更なしで適用する一気通貫テストを追加(`packages/ashura-surveyor/test/self-model-bootstrap.test.ts`、
      Phase 4の`ashura-dialect-ui`と同型のパターン)。人間ゲート(承認そのもの)は自動化せず、
      承認済みになった後のゲート・トレース表完全性・欠落分析が機能することのみを検証する

## Phase 6 — ファシリテーターの対話実装(最小構成のみ完了)

- [x] 対話そのもの(LLM由来、非決定的)を `DialogueAgent` seam の裏に隔離して実装した。
      既存の `analyzeGaps`/`FacilitationQuestion`(禁止遷移、決定的)は変更していない(additive only)。
      Phase 4/5 の一気通貫テストが実証した「既存サーベイヤー関数がコード変更なしで動く」性質を壊さない
- [x] LLM由来の追加質問トピックは「失敗系」1件のみに絞った(境界規則は将来フェーズ送り。
      Phase 0「examples 4題を一気にやらない」/ Phase 4「trait 1つに絞る」と同じ判断)
- [x] 閉ループの実証: 禁止遷移0件の草稿 → `analyzeGaps` が質問1件 → フィクスチャ `DialogueAgent`
      が回答 → 草稿更新(`.ashura` テキスト再生成 + 再パース) → 再度 `analyzeGaps` が0件、を
      1本の統合テストで確認した(Phase 1 の乖離ライフサイクル配線漏れ・Phase 4 の
      `assertNoParseErrors` と同じ「テストは緑だが半分のパイプライン」を防ぐ)
- [x] 人間ゲート: セッションが触れてよいのは草稿まで。`モデル成果物` の状態遷移
      (検査通過/レビュー中/承認済み)を進める処理は書いていない

**合否基準**: 禁止遷移0件の草稿に対して対話セッションを実行すると、フィクスチャ回答から
草稿が更新され、再測量で欠落が解消したことが機械的に確認できる。← 統合テストで確認済み。
LLM由来の値(`declarationText`)は「AI出力不信」原則に基づき形式検査を経てから草稿に反映される
(挿入先はLLM出力ではなく `analyzeGaps` が生成する決定的な `scope` から決定)。実LLM配線・
境界規則トピックの追加は将来フェーズ送り。

## 非目標

- UML互換 / 図の自動レイアウト(ビューはテキストとdiffが主、図は派生物)
- 完全事前仕様の要求(漸進的記述が原則。フローだけで始めてよい)
- 人間承認の自動化(いかなるフェーズでも行わない)
