/**
 * domain/ashura.model.ashura 文脈「モデル編集」集約「モデル成果物」の状態の最小表現。
 * 状態: 草稿 -> 検査通過 -> レビュー中 -> 承認済み。
 *
 * 遷移ロジック(契機・禁止遷移)を持つ集約そのものはPhase 3では実装しない
 * (生成ゲートが必要とするのは「承認済みかどうか」の判定のみであり、
 * 遷移条件の検査は他のフェーズ/コンテキストの責務。投機的な抽象化を避ける)。
 */
export type ModelStatus = '草稿' | '検査通過' | 'レビュー中' | '承認済み';
