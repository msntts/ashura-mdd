# CLAUDE.md — Ashura MDD 作業引き継ぎ

このリポジトリは「AI時代のモデル駆動開発フレームワーク Ashura」の初期構想。
スマホ版Claudeでの設計議論から生まれた。**このファイルを読めば会話履歴なしで作業を継続できる。**

## 最初に読む順序

1. `README.md` — 思想の全体像(ビュー切り替え、三点測量、6つの中核原理)
2. `domain/ashura.model.ashura` — **この環境自身の仕様**。仕様の真実はここのみが持つ(モデル権威、機械可読)
3. `ROADMAP.md` — 実装順序と各Phaseの合否基準
4. `docs/decisions/` — 決定済み事項。**不採用案も書いてあるので同じ議論を蒸し返さない**
5. `examples/` — DSL表現力の実験記録4題(構文の当たりは付いている)

## いまの状態・次のタスク

現在のフェーズと次タスクは `ROADMAP.md` を正とする(ここには書かない。二重管理は同期コストを生むため)。

## 設計上の絶対規則(ADRで確定済み)

- **方言は継承ではなく脱糖で拡張する**(ADR-0002)。UI固有構文などをコア文法に混ぜない。
  全方言構文は脱糖規則を持ち、サーベイヤーはコア語彙のみを見る
- **決定性境界**: LSP検査は決定的。LLM推論をLSP検査に混ぜない
- **人間ゲート**: モデルの承認・乖離の裁定を自動化するコードを書かない
- **モデル権威**: 仕様に関する疑問は `domain/ashura.model.ashura` を正とする。
  READMEとモデルが食い違ったらREADMEを直す

## 議論済みで結論が出ている論点(再検討不要)

- 人間がレビューするのは構造(クラス図等)ではなく **振る舞いと制約**。
  構造はAIに委ね、非機能要件が構造を強制する場合のみ制約として書く
- モデルに書く基準: **「その記述を変えたときビジネス上の意味が変わるか」**。
  変わらないもの(リトライ回数等)は `steering/` へ
- エラー処理: ドメインの失敗(残高不足等)はモデルに書く。技術の失敗(タイムアウト等)は
  個別に書かず、失敗時の性質(冪等/原子性/補償/劣化許容)だけを契約として書く
- 状態機械の置き場所は **集約**(フローではない)。フローのイベントが遷移をトリガーする参照関係
- MDDの死因は表現力ではなく **同期コスト**。だからサーベイヤー(drift検出)を最優先で作る
- スケール対応: 合成規則は部品対ではなく **種別(役割)** の上に書いて O(n²) を殺す。
  状態機械の重複は trait(基底状態機械の取り込み)で解決 — これらは方言側(Phase 4)の課題

## 用語対応(会話由来の独自語彙)

- **三点測量**: ①意図(承認済みモデル) ②実装(コード由来推論) ③解釈(AI独立導出) の突き合わせ。
  診断は `domain/ashura.model.ashura` の決定表「差分診断」参照
- **サーベイヤー**: 三点測量と乖離検出を行うエージェント。測量器
- **アドバーサリー**: モデルの宣言(性質・失敗セマンティクス)を攻撃する検証エージェント。
  **コードを見てテストを作ってはならない**(独立導出=共謀防止)
- **脱糖(desugar)**: 方言構文をコア語彙に還元する変換。方言プラグインの必須要件
- **優雅な縮退**: アルゴリズム系などモデルで書けない領域では契約と性質だけに薄くなること。器は壊れない

## 技術スタック(steering)

- TypeScript / Langium(DSL+LSP。Langium自身がセルフホスティングの伝統を持つ)
- モノレポ(packages/)。コアの変更はレビューゲートを重くする
- 検証系(Phase 2以降): プロパティベーステスト(fast-check想定) / フォールトインジェクション

## やってはいけないこと

- コア文法にドメイン固有構文(トークン、盤面など)を追加する → 方言へ
- `.ashura` 文法を4題材ぶん一気に作る → すごろくで最小集合を固めてから
- 仕様の疑問をREADMEやこのファイルで解決する → モデルが正
- examplesのMarkdownを「ドキュメント」として整形し直す → あれは実験記録。凍結扱い
